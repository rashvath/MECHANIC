import { MechanicRouteShell } from "@/components/layout/mechanic-route-shell";

export default function MechanicLayout({ children }: { children: React.ReactNode }) {
  return <MechanicRouteShell>{children}</MechanicRouteShell>;
}
